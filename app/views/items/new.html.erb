<h1>New item in <%= @list.name %></h1>

<%= form_for [@list, @list.items.build] do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
